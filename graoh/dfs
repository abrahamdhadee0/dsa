#include <bits/stdc++.h>
using namespace std;

class solution{
 void dfs(int node,vector<vector<int>>& adj,vector<int<& vis,vecotr<int>& ls){
    vis[node]=1;
    ls.push_back(node);
    for(auto it: adj[node]){
        if(!vis[it]){
            dfs(it, adj, vis, ls);
        }
    }
 }
}

vector<int> dfs(vector<vector<int>>& adj){
    int n=adj.size();
    vector<int> vis(n,0);
    vector<int> ls;
    int start=0;
    dfs(start, adj, vis, ls);
    return ls;
}
int main(){
    int n,m;
    cin>>n>>m;
    vector<vector<int>> adj(n);
    for(int i=0;i<m;i++){
        int u,v;
        cin>>u>>v;
        adj[u].push_back(v);
        adj[v].push_back(u);
    }
    solution ob;
    vector<int> ans=ob.dfs(adj);
    for(auto it: ans){
        cout<<it<<" ";
    }
}